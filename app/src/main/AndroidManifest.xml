<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.convertit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_convertit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_convertit"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConvertIt">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WeightActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".LengthActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".TempActivity"
            android:parentActivityName=".MainActivity"/>


    </application>

</manifest>